{
  "name": "ai-messenger-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "dev": "echo \"Use: npm run dev:backend and npm run dev:frontend\"",
    "dev:backend": "npm --workspace apps/backend run dev",
    "dev:frontend": "npm --workspace apps/frontend run dev",
    "build": "npm -w apps/backend run build && npm -w apps/frontend run build",
    "start": "npm -w apps/backend run start",
    "lint": "npm -w apps/backend run lint && npm -w apps/frontend run lint",
    "format": "npm -w apps/backend run format && npm -w apps/frontend run format"
  }
}
